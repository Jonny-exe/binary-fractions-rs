#[path = "../src/binary.rs"]
mod binary;
mod tests {
    // Note this useful idiom: importing names from outer (for mod tests) scope.
    #[test]
    fn test_Binary() {
        assert!(true);
    }
}
